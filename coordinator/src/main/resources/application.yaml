server:
  port: 8080

spring:
  application:
    name: coordinator
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/kintsugi
    username: ${DB_USER:kintsugi}
    password: ${DB_PASSWORD:kintsugi}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,metrics